---
author: Mainasara Tsowa
pubDatetime: '2019-03-11T19:15:38.000Z'
title: Avoiding Weird Javascript Behaviour (true + true === 2 but true !== 1)
featured: true
description: Explaining some weird behavior in javascript and how to avoid it
tags:
  - javascript
  - nodejs
draft: false
---
Every single programming language is designed to be intuitive and Javascript is no exception, but it does have some crazy quirks that make it stand out, one of which is its weird behavior with types and I am pretty sure you have seen the memes. This can be traumatizing the first time you see it but it does not have to be, so I want to shed a little light on it and help you avoid running into such. How Javascript types work Javascript is a very very loosely typed language, this is not bad at all but in some situations, it can cause very bad head scratching. One of these moments occurred when I first saw this a few years back. At first glance, it looks very *wrong* but don't fret there is nothing wrong here, that is just how javascript works. Javascript handles types in a very different way, it has only 6 primitive types and all the code you write is represented by one of them. This alone is not enough to cause such strange behavior but the way they are handled does, javascript always converts types to best fit what they are being used for, this is called **Type Coercion** and sometimes it changes value types to those not intended by the developer, this is why . The binary operator is primarily meant to add numbers but when it is faced with boolean values it's forced to convert them to numbers thus becomes which makes become and the expression is turned into But the second expression seems to defeat what I just explained above but it really makes perfect sense too. The operator performs a strict comparison so it checks both value and type but since is a boolean and is a number they are not strictly equal thus the expression is true. Truthy and Falsy Every value in javascript has its own boolean value (truthy/falsy), these values are used in operations where a boolean is expected but not given, you have most likely used this feature before but never knew what was going under the hood. Example: In the code above, is not a boolean but because the value is "truthy", the if block will be executed. Falsy vs Falsy values are values with an inherent boolean , the following are falsy values. 0 '' or "" null undefined NaN Example: Note that the value is falsy but falsy values are not and the right way to differentiate them is by using **strict comparison** operators. Example: Truthy vs Every value that is not is considered , these include strings arrays objects functions Example: Type coercion Type coercion is the process of converting one type to another and every value in javascript can be coerced. There two types of coercion: implicit: This is done automatically when the need arises e.g The above example shows implicit coercion in its most recognizable form, the operator is not only used to add numbers but strings too so when it is told to add a number to a string it has to convert the number to a string first, which changes to and the expression becomes. explicit: This is when a developer converts types by writing the appropriate code to do so e.g Type conversions When a value is coerced, it undergoes one of three conversions. ToString: This is triggered implicitly by the operator and explicitly by calling the function e.g The operator implicitly converts the floating point number to a string before concatenating them. Here we use the function to explicitly convert the floating point number to a string. ToBoolean: This is triggered implicitly by either their context or by logical operators ( , and ) and explicitly by the function. Note that the operator does not return the truthy value of but the actual string itself ToNumber: Numeric conversion is very tricky because it is triggered by a lot of operators . Note, that binary does not trigger numeric conversion and does not trigger numeric conversion when both operands are strings. There is a lot more on coercion that I can't explain here, so here is a link to an excellent post on javascript type coercion. Avoiding this behavior The best way to avoid this behavior is by type checking and an easy way to do this in vanilla javascript is by using a strict equality operator when comparing values or checking a variable's type with the keyword. Example: **without type checking** **with type checking** Unfortunately, the examples above can not help you with checking classes, for that you need to use the keyword . But by far the easiest way to avoid this is by using Typescript. If you haven't heard of it, Typescript is a typed superset of javascript that compiles to normal javascript. In short, Typescript and its compiler help you write code that is very deterministic and I encourage you to click the link if you want to know more because the site explains more than I can. Thanks for reading!!! _Consider giving me a follow on Twitter_
