---
author: Mainasara Tsowa
pubDatetime: '2019-03-04T16:07:03.000Z'
title: Using CSS Selectors In Javascript
featured: true
description: A guide to fully understanding CSS selectors in javascript
tags:
  - css
  - javascript
  - html
  - webdev
draft: false
---
In CSS, selectors are patterns used to select the element(s) you want to style, but as you can tell from the title above, selectors are also useful in javascript and below are some examples on how to use them. Basics Using a selector in javascript Use the method To get all matching elements, use the method By IDs To get an element by its ID, use a before the element ID By classes To get elements by class, use a before the class name By tag name To get elements by tag name, just input the tag name Replicating and Replicating Replicating All elements To get all elements use Using multiple selectors To use multiple selectors, we seperate each of them with a . More with elements In the above examples, we made basic queries, but other things can be done like getting elements by order or parent. Getting element children There are two variants of this, one gets an element's child no matter how deep it is down the tree, and the other gets an element's direct child. With the above HTML as an example, we will look at these two variants. Direct child All children Getting elements by order There are two ways to do this also. Consider the following HTML. Placed after Preceded by With , it does not matter the element immediately behind matches. and we can see that the element did not affect the result because it does not matter if the was there in the first place. But the following selector will fail because it checks the element immediately above it. Getting elements by attribute An attribute selector starts with and ends with and is used as such Check attribute value To check an attribute value we use the symbol. More with attribute values Check if attribute starts with... Check if attribute ends with... Check if the attribute contains a substring Advanced selectors :focus This selects the element that currently has focus :visited This is used with tags and selects links that have been visited :link This is also used with tags but in this case, selects links that have not been visited :enabled This selects elements that are enabled(_not disabled_) :disabled This selects elements that have been disabled :first-child This selects the element that is the first child of its parent :last-child This selects the element that is the last child of its parent _el_:first-of-type This selects the element that is the first child of its parent and is the same type as _el_ _el_:last-of-type This selects the element that is the last child of its parent and is the same type as _el_ :not(_selector_) This selects elements that don't match the selector :nth-child(_n_) This selects the element that is the _n_ th child of its parent. :nth-last-child(_n_) This selects the element that is the _n_ th to the last child of its parent. Mix and match These selectors can be mixed together to perform some complex checks. e.g A disabled button of class 'btn' All disabled buttons in a All disabled buttons in a and all buttons outside it All links to external pages And to get links that are not to external pages, use Conclusion These are just some of the ways you can use selectors in javascript and if you want to know more, here is a link to a CSS selector reference on w3c. Thanks for reading!!!
